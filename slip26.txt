1. Write a Java program to delete the details of given employee (ENo EName Salary).
Accept employee ID through command line. (Use PreparedStatement Interface)



Open psql:

CREATE TABLE employee(
    eno INT PRIMARY KEY,
    ename VARCHAR(50),
    salary INT
);



Insert sample data:

INSERT INTO employee VALUES(1,'Rahul',25000);
INSERT INTO employee VALUES(2,'Amit',30000);
INSERT INTO employee VALUES(3,'Sneha',28000);





Save as DeleteEmployee.java


import java.sql.*;

public class DeleteEmployee {
    public static void main(String[] args) {
        try {
            Class.forName("org.postgresql.Driver");

            Connection con = DriverManager.getConnection(
                    "jdbc:postgresql://localhost:5432/testdb",
                    "postgres",
                    "password"
            );

            int eno = Integer.parseInt(args[0]);

            PreparedStatement pst = con.prepareStatement(
                    "DELETE FROM employee WHERE eno = ?"
            );

            pst.setInt(1, eno);

            int i = pst.executeUpdate();

            if (i > 0)
                System.out.println("Employee record deleted successfully.");
            else
                System.out.println("Employee not found.");

            con.close();
        } catch (Exception e) {
            System.out.println(e);
        }
    }
}



javac -cp ".:postgresql-42.7.3.jar" DeleteEmployee.java

java -cp ".:postgresql-42.7.3.jar" DeleteEmployee 2



Output
Employee record deleted successfully.

If employee id not found:

Employee not found.





2. Write a JSP program to calculate sum of first and last digit of a given number. Display
sum in Red Color with font size 18. 



Save as sum.jsp



apache-tomcat
 └── webapps
      └── sumapp
           └── sum.jsp


<html>
<body>

<h2>Sum of First and Last Digit</h2>

<form method="post">
Enter Number:
<input type="text" name="num">
<br><br>
<input type="submit" value="Calculate">
</form>

<%
String n = request.getParameter("num");

if(n != null)
{
int num = Integer.parseInt(n);

int last = num % 10;

while(num >= 10)
{
num = num / 10;
}

int first = num;
int sum = first + last;
%>

<p style="color:red; font-size:18px;">
Sum = <%= sum %>
</p>

<%
}
%>

</body>
</html>


------------------------------------------
Open browser
http://localhost:8080/sumapp/sum.jsp
Example

Input:

1234

Output:

Sum = 5